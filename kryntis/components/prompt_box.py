"""Prompt Box input component."""

class PromptBox:
    """Manages input prompting, multiline buffers, and autocomplete."""
    @staticmethod
    def format_prompt(indicator: str = ">>> ") -> str:
        return indicator
